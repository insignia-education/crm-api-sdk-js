# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**`AGENTS.md`** (same directory) is the canonical, agent-readable source of project guidance —
read it first. Everything else — architecture, versioning, the API ↔ SDK sync rule, testing
requirements, the i18n rule, deployment, git safety, the "Never do" list, working style, and
communication style — lives there or under `.ai/` (`.ai/guidelines/`, `.ai/docs/`) and is not
repeated here.

No Claude-Code-tool-specific instructions apply beyond that yet.
